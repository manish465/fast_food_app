import { NavBar, TrendingContainer } from "../components";

const Home = () => {
    return (
        <>
            <NavBar />
            <TrendingContainer />
        </>
    );
};

export default Home;
