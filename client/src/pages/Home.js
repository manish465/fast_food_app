import { NavBar, PopularContainer, TrendingContainer } from "../components";

const Home = () => {
    return (
        <>
            <NavBar />
            <TrendingContainer />
            <PopularContainer />
        </>
    );
};

export default Home;
